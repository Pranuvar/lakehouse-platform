# Deps for seeders/ only (data generation + loading). Kept separate from
# any Airflow/Spark requirements since these run from the host, not a
# container, and don't need Java/PySpark at all.
#
# Lower bounds only (not exact pins): tested against numpy 2.5 / pandas 3.0
# on Python 3.14, but nothing here depends on version-specific behaviour.
numpy>=1.26
pandas>=2.2
psycopg2-binary>=2.9
Faker>=30.0
pyarrow>=17.0
boto3>=1.35
kafka-python-ng>=2.2
requests>=2.32
